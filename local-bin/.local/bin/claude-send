#!/bin/bash
# Loads annotation buffer into tmux paste buffer and clears the file.
# Called from tmux binding; paste-buffer runs in the binding itself.

BUFFILE="/tmp/claude-buf.md"

[ ! -s "$BUFFILE" ] && exit 1

tmux load-buffer "$BUFFILE"
> "$BUFFILE"
