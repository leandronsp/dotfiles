.DS_Store
*.swp
*.swo

# Override global gitignore for files we track in this repo
!.tool-versions
.claude/settings.json
.claude/scheduled_tasks.lock
cortex/
scratch/

# Compiled Swift tool — rebuild with `make build`
local-bin/.local/bin/image-use
